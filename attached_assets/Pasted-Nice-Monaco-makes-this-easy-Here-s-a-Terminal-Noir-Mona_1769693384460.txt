Nice — Monaco makes this easy. Here’s a **Terminal Noir Monaco theme** that matches your UI tokens (amber = intent, cyan = AI *outside* editor), keeps code the focus, and avoids the “VS Code blue” vibe.

## 1) Create `client/src/monaco/terminalNoirTheme.ts`

```ts
// client/src/monaco/terminalNoirTheme.ts
import type * as monaco from "monaco-editor";

export const TERMINAL_NOIR_THEME_NAME = "terminal-noir";

/**
 * Terminal Noir (Monaco)
 * - Editor stays sacred: no gradients, no glow, no cyan accents.
 * - Amber is used only for focus/selection/cursor.
 * - Neutral, high-contrast text with muted UI-like chrome.
 */
export function defineTerminalNoirTheme(m: typeof monaco) {
  m.editor.defineTheme(TERMINAL_NOIR_THEME_NAME, {
    base: "vs-dark",
    inherit: true,

    rules: [
      // -------- Core syntax (restrained, professional) --------
      { token: "", foreground: "E6EAF2" }, // default text
      { token: "comment", foreground: "6E7888", fontStyle: "italic" },
      { token: "string", foreground: "C7F9D4" },
      { token: "number", foreground: "FFD17A" },
      { token: "keyword", foreground: "B9A6FF", fontStyle: "bold" },
      { token: "type", foreground: "8ED0FF" },
      { token: "delimiter", foreground: "9AA4B2" },
      { token: "operator", foreground: "9AA4B2" },

      // Functions / vars: keep readable, not neon
      { token: "identifier", foreground: "E6EAF2" },
      { token: "variable", foreground: "D7DDEE" },
      { token: "function", foreground: "E9ECF5", fontStyle: "bold" },

      // JSON keys, params, etc.
      { token: "key", foreground: "E6EAF2" },

      // Errors
      { token: "invalid", foreground: "FF5C5C" },
    ],

    colors: {
      // -------- Surfaces --------
      "editor.background": "#0C1020",
      "editor.foreground": "#E6EAF2",
      "editorLineNumber.foreground": "#6E7888",
      "editorLineNumber.activeForeground": "#9AA4B2",

      // -------- Cursor + selection (amber intent) --------
      "editorCursor.foreground": "#FFB454",
      "editor.selectionBackground": "#2A231A", // amber-tinted low intensity
      "editor.inactiveSelectionBackground": "#1C1A18",
      "editor.selectionHighlightBackground": "#2A231A",
      "editor.wordHighlightBackground": "#1A223A",
      "editor.wordHighlightStrongBackground": "#232E4A",

      // -------- Current line / indent guides --------
      "editor.lineHighlightBackground": "#0F1426",
      "editor.lineHighlightBorder": "#0F1426",
      "editorIndentGuide.background1": "#1F2A44",
      "editorIndentGuide.activeBackground1": "#232E4A",

      // -------- Find / match --------
      "editor.findMatchBackground": "#2A231A",
      "editor.findMatchHighlightBackground": "#1C1A18",
      "editor.findRangeHighlightBackground": "#111726",

      // -------- Brackets --------
      "editorBracketMatch.background": "#151C2E",
      "editorBracketMatch.border": "#FFB454",

      // -------- Rulers / whitespace --------
      "editorWhitespace.foreground": "#1F2A44",
      "editorRuler.foreground": "#1F2A44",

      // -------- Gutter / minimap --------
      "editorGutter.background": "#0C1020",
      "minimap.background": "#0B0E14",

      // -------- Suggest / hover (subtle, not loud) --------
      "editorSuggestWidget.background": "#111726",
      "editorSuggestWidget.border": "#1F2A44",
      "editorSuggestWidget.foreground": "#E6EAF2",
      "editorSuggestWidget.selectedBackground": "#151C2E",

      "editorHoverWidget.background": "#111726",
      "editorHoverWidget.border": "#1F2A44",

      // -------- Diagnostics --------
      "editorError.foreground": "#FF5C5C",
      "editorWarning.foreground": "#FFC857",
      "editorInfo.foreground": "#7C8CFF",
    },
  });
}
```

---

## 2) Wire it into your Monaco setup

Wherever you create the editor (Monaco React wrapper or manual), do:

```ts
import * as monaco from "monaco-editor";
import { defineTerminalNoirTheme, TERMINAL_NOIR_THEME_NAME } from "./monaco/terminalNoirTheme";

defineTerminalNoirTheme(monaco);
monaco.editor.setTheme(TERMINAL_NOIR_THEME_NAME);
```

### If you use `@monaco-editor/react`

```tsx
import Editor from "@monaco-editor/react";
import { defineTerminalNoirTheme, TERMINAL_NOIR_THEME_NAME } from "./monaco/terminalNoirTheme";

<Editor
  theme={TERMINAL_NOIR_THEME_NAME}
  beforeMount={(m) => defineTerminalNoirTheme(m)}
  options={{
    fontFamily: '"JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace',
    fontSize: 13,
    lineHeight: 20,
    minimap: { enabled: true },
    scrollBeyondLastLine: false,
    renderLineHighlight: "line",
    cursorBlinking: "solid",
    cursorSmoothCaretAnimation: "on",
    smoothScrolling: true,
    padding: { top: 10, bottom: 10 },
  }}
/>
```

---

## 3) Keep AI Cyan out of the editor (important)

Use **cyan only in the AI panel** and agent status UI. If cyan leaks into syntax colors, it starts feeling like a “themed IDE” instead of a terminal-grade tool.

If you *do* want a tiny nod to “AI” in-editor, limit it to one place only:

* inline suggestion ghost text (very faint cyan)
* or AI annotations in the gutter

I can give you that too if you’re using Monaco inline completions.

---

## 4) Optional: Make Monaco read your CSS tokens

If you want Monaco colors to match your `:root[data-theme="terminal-noir"]` values exactly, tell me how you mount Monaco (plain `monaco.editor.create` vs `@monaco-editor/react`) and I’ll show the cleanest pattern to pull computed CSS vars and feed them into `defineTheme()`.

If you paste the file where you initialize Monaco (or tell me which wrapper you’re using), I’ll map it 1:1 into your project structure with minimal changes.
