# SimpleAide Docker Environment Configuration
# Copy this file to .env.docker and customize as needed

# ===========================================
# Application Settings
# ===========================================
PORT=8521
HOST=0.0.0.0
NODE_ENV=production

# Session secret (REQUIRED - generate a secure random string)
# Example: openssl rand -hex 32
SESSION_SECRET=replace-me-with-a-long-random-string

# ===========================================
# Ollama Configuration
# ===========================================
# Internal Docker network URL (do not change unless you modify docker-compose)
OLLAMA_BASE_URL=http://ollama:11434

# Default model to pull on startup
OLLAMA_MODEL=qwen2.5:7b

# Request timeout in milliseconds (default: 120000 = 2 minutes)
OLLAMA_REQUEST_TIMEOUT_MS=120000

# ===========================================
# Optional: External API Keys
# ===========================================
# Uncomment and fill in if using external services

# KAGGLE_USERNAME=your_kaggle_username
# KAGGLE_KEY=your_kaggle_api_key

# HUGGINGFACE_API_KEY=your_hf_api_key

# NGC_API_KEY=your_nvidia_ngc_api_key

# ===========================================
# Optional: GPU Configuration
# ===========================================
# Uncomment to pin Ollama to a specific GPU (0, 1, etc.)
# CUDA_VISIBLE_DEVICES=0
